# Стартовый промпт для новой беседы о Mini App

Подключите к новой беседе корневую папку репозитория:

```text
C:\Users\ibogdanovskaia\Desktop\Яна\Мастер CFO\Проекты\Чат-бот_календарь
```

Не подключайте только вложенную папку `miniapp`: новой беседе нужен доступ к backend, общим тестам и документации. При этом все материалы и код нового интерфейса должны сохраняться внутри `miniapp/`.

Скопируйте в новую беседу следующий текст:

---

Ты продолжаешь существующий проект Telegram Calendar Bot и начинаешь отдельный подготовительный этап Telegram Mini App.

Перед любыми действиями полностью прочитай:

1. `AGENTS.md`;
2. `PROJECT_CONTEXT.md`;
3. `README.md`;
4. `CI_CD_инструкция.md`;
5. `ТЗ_Telegram-бот_Google-Calendar_v1.1.md`;
6. `miniapp/AGENTS.md`;
7. `miniapp/README.md`.

Работай в ветке `dev` или создай `feature/miniapp-*` от `dev`. Ветка `main` соответствует production. Не переноси изменения в `main` и не развёртывай их без моего отдельного подтверждения.

Существующий Telegram-бот, Google Calendar, база, сайт, навигация и внешний вид должны продолжать работать без изменений. Не программируй сразу: сначала проверь текущую архитектуру, проведи интервью по Mini App, предложи 2–3 варианта frontend-стека и API с рекомендацией, затем подготовь архитектуру, карту экранов, этапы реализации и критерии приёмки.

Mini App должен быть статическим frontend, собираться на GitHub Actions и работать без Docker и постоянного Node.js runtime на VPS. Он использует существующий backend и единую бизнес-логику. Telegram `initData` обязательно проверяется сервером. Пользователь не должен видеть содержимое личного Google Calendar владельца.

Все новые документы и frontend-код сохраняй в `miniapp/`. Изменения вне этой папки сначала обосновывай и согласовывай. Проверяй всё самостоятельно и обращайся ко мне только тогда, когда без моего решения или внешней авторизации нельзя продолжить.

Отдельно учти открытый инфраструктурный вопрос из `PROJECT_CONTEXT.md`: CI работает, но GitHub Environment, secrets, защита `main` и end-to-end CD ещё нужно завершить после авторизации владельца.

На первом шаге ничего не программируй. Подготовь список уточняющих вопросов и рекомендуемый план проектирования Mini App.

---
